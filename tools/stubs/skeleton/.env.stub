# Fzr Environment Variables
# Local values here override app.ini
#
# ── Project Info (generated by `php tool init`) ───────────────────
# App      : {{APP_NAME}}
# Timezone : {{TIMEZONE}}
{{DEPLOY_MEMO}}# ─────────────────────────────────────────────────────────────────

APP_DEBUG={{DEBUG}}

{{DB_CONFIG_REAL}}

# Redis セッション/キャッシュを使う場合は以下を有効化
# REDIS_HOST="localhost"
# REDIS_PORT=6379
